Feature-Level Fusion (The "True Merge")
This is the most integrated approach. The goal is to replace the simple Xception features in Model 2 with the more advanced "Xception-with-Attention" features from Model 1, and then feed this new, superior feature set into the rest of Model 2's pipeline.

Here is the step-by-step process:

Train Model 1 (Xception+Attention): Use your first notebook (deepfake-detection2...ipynb) to train and save the best_model.keras.

Train Model 2 (Backbones): Use your second notebook (feature-selection-aided...ipynb) to fine-tune and get the DenseNet121 and EfficientNetB0 models.

Create a New Hybrid Feature Extractor: You will modify the feature extraction part of Notebook 2. Instead of three separate extractors, you will create a new one that combines features from all models:


Features A (from Model 2): Get the fine-tuned DenseNet121 and extract its Global Average Pooling (GAP) features (1024 dimensions).


Features B (from Model 2): Get the fine-tuned EfficientNetB0 and extract its GAP features (1280 dimensions).

Features C (from Model 1): Load your best_model.keras from Notebook 1. Create a new model whose input is the same, but its output is the att tensor (the output of the Attention layer in your model.py, right before it goes to the final Dense(2, 'softmax') layer). This will give you an attention-weighted feature vector of 361 dimensions (dim**2, where dim=19).

Create Your New Stacked Vector: For each image, extract Features A, B, and C. Then, concatenate them into a single vector.

New Feature Vector Dimension: 1024 (DenseNet) + 1280 (EfficientNet) + 361 (Xception+Attention) = 2665 features.

Apply Model 2's Pipeline: Take this new 2665-feature vector and feed it into the rest of Notebook 2's process:

Run the Feature Selection (ReliefF, MI, mRMR, and the Inclusion-Exclusion algorithm). This will select the most discriminative features from your new hybrid set.


Train the final KNN Classifier on these selected features.



combining the diverse feature sets of DenseNet and EfficientNet with the artifact-focused features of our Xception+Attention model. This creates a much richer "super-vector" for the feature selection and KNN classifier to work with.


My Kaggle dataset structure in test, each fake and real folder has 1200 PNGs
In the train folder fake folder has 6028 PNG, and the real folder has 5605 PNG
And in validation, also each fake and real folder has 1200 PNGs

ex-
!ls /kaggle/input/1000-videos-split/1000_videos/train/fake



1000_videos/

├── test/

│   ├── fake/

│   │   ├── 067_025_1.png

│   │   ├── 067_025_2.png

│   │   └── ...

│   └── real/

│       ├── 067_16.png

│       ├── 067_17.png

│       └── ...

├── train/

│   ├── fake/

│   │   ├── 128_896_3.png

│   │   ├── 128_896_4.png

│   │   └── ...

│   └── real/

│       ├── 129_2.png

│       ├── 129_3.png

│       └── ...

└── validation/

    ├── fake/

    │   ├── 000_003_0.png

    │   ├── 000_003_1.png

    │   └── ...

    └── real/

        ├── 000_0.png

        ├── 000_1.png

        └── ...



I want a fully structured, easy-to-update code for this project to achieve 99%+ accuracy

Give me modular code for my Kaggle notebook, and give me each cell code in a different canvas i mean each module like basic blcks model,train,utile,main and others modules and at the end prediction canvas.
as per requirement use "%%writefile" for proper integration in between cells follow my attention models code base 




seperate canvas for each cell of notebook not in one its a mess

i want cell code like "import os

import glob

import random

import cv2

import numpy as np

import pandas as pd

import matplotlib.pyplot as plt

import seaborn as sns

import tensorflow as tf

from sklearn.neighbors import KNeighborsClassifier

from sklearn.feature_selection import mutual_info_classif

from sklearn.metrics import (

    accuracy_score, roc_auc_score, confusion_matrix, 

    classification_report, f1_score"



not like ""cell_type": "code",

   "execution_count": null,

   "metadata": {},

   "outputs": [],

   "source": [

    "import tensorflow as tf\n",

    "import numpy as np\n",

    "import pandas as pd\n",

    "import os\n",

    "import glob\n",

    "import joblib\n",

    "import random\n",

    "import cv2\n",

    "import warnings\n",

    "\n","

